1.利用group字段区分各种方式的使用

一个group代表一个图片验证码实例,一般取servlet中配置的名称

那不如使用instanceId来代表一个图片验证码实例！！！

这个instanceId


2.弹出式图片验证码和其他两种不同，这个需要进行弹出式

2.1. 需要提供js方面的api来进行弹出对话框，还要能够提供js通知接口
2.2. 前端页面跟前面两种方式有很大差别

弹出式图片验证码，需要渗透到java代码里面???

一定不能渗透到产品系统的java代码里面，否则后期很难维护，想改都改不了

只能配置servlet的时候注册一个地址

targetRequest


